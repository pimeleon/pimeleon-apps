---
# pi-router-apps — GitLab CI/CD entry point
# Lightweight pipeline: lint + upstream version checking only.
# Package builds and publishing run on GitHub Actions (see .github/workflows/).

include:
  - local: '.gitlab/ci/workflow.yml'
  - local: '.gitlab/ci/lint.yml'
  - local: '.gitlab/ci/check-versions.yml'
