---
# GitLab CI/CD — Build & Verification

build:smoke:
  stage: build
  image: docker:latest
  tags:
    - containers
  needs:
    - job: "lint:shell"
    - job: "lint:yaml"
    - job: "security:gitleaks"
    - job: "security:trivy:repo"
    - job: "security:trivy:builder-armhf"
    - job: "security:trivy:builder-arm64"
  interruptible: true
  variables:
    DOCKER_HOST: unix:///var/run/docker.sock
  parallel:
    matrix:
      - TARGET_ARCH: ["armhf", "arm64"]
  script:
    # Basic verification: Ensure the builder environment can be initialized
    - apk add --no-cache make bash curl
    - make refresh-tools
    - make factory-init TARGET_ARCH=$TARGET_ARCH
  cache:
    policy: pull
  rules:
    # Run on develop branch push
    - if: '$CI_COMMIT_BRANCH == "develop"'
    # Run on MRs targeting develop (or any branch except master)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME != "master"'

# Full build for production branch (master) or MRs targeting master
build:all:
  stage: build
  image: docker:latest
  tags:
    - containers
  needs:
    - job: "lint:shell"
    - job: "lint:yaml"
    - job: "security:gitleaks"
    - job: "security:trivy:repo"
    - job: "security:trivy:builder-armhf"
    - job: "security:trivy:builder-arm64"
  interruptible: true
  variables:
    DOCKER_HOST: unix:///var/run/docker.sock
  parallel:
    matrix:
      - TARGET_ARCH: ["armhf", "arm64"]
  script:
    - apk add --no-cache make bash curl
    - make refresh-tools
    - make factory-init TARGET_ARCH=$TARGET_ARCH
    - ./local-build.sh all $TARGET_ARCH
  artifacts:
    paths:
      - output/
      - logs/
    expire_in: 1 week
    when: on_success
  rules:
    # Run on direct push or merge to master
    - if: '$CI_COMMIT_BRANCH == "master"'
    # Run on MRs targeting master to verify full build before merge
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "master"'
