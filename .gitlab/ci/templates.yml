---
# GitLab CI/CD — Build & Verification

# Template for builder image creation
.build_builder_base:
  image: docker:latest
  services:
    - docker:dind
  tags:
    - containers
  needs:
    - job: "check:upstream-versions"
      optional: true
    - job: "security:gitleaks"
      optional: true
  variables:
    DOCKER_HOST: unix:///var/run/docker.sock
    GITLAB_DEPLOY_TOKEN: ${GITLAB_DEPLOY_TOKEN}
  rules:
    # 1. Always run on master (ensure production images are fresh/pushed)
    - if: '$CI_COMMIT_BRANCH == "master"'
      changes: !reference [.builder_rebuild_globs, changes]

    # 2. On develop or other branches: Only run if builder sources actually changed
    - if: '$CI_COMMIT_BRANCH == "develop" && $CI_PIPELINE_SOURCE == "push"'
      changes: !reference [.builder_rebuild_globs, changes]

    # 3. Run on MR to develop or master if builder sources changed (pre-merge gate)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^(develop|master)$/'
      changes: !reference [.builder_rebuild_globs, changes]

    # 4. Always run on manual trigger from web/schedule
    - if: '$CI_PIPELINE_SOURCE == "web" || $CI_PIPELINE_SOURCE == "schedule"'
  script:
    - apk add --no-cache bash curl
    - chmod +x scripts/publish-builders.sh
    - ./scripts/publish-builders.sh $TARGET_ARCH

build:builder-armhf:
  extends: .build_builder_base
  stage: build-builder
  variables:
    TARGET_ARCH: "armhf"

build:builder-arm64:
  extends: .build_builder_base
  stage: build-builder
  variables:
    TARGET_ARCH: "arm64"
