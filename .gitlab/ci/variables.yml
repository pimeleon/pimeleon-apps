---
# GitLab CI/CD — Global Variables
default:
  interruptible: true

variables:
  GIT_DEPTH: 0
  DEFAULT_ARCH: "armhf"

.common_file_globs:
  changes: &common_globs
    - "src/**/*"
    - "scripts/**/*.sh"
    - ".gitlab/ci/**/*"
